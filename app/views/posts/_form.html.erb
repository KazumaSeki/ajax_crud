<%= form_with model: post, local: false do |f| %>
  <%= f.text_field :content %>
  <%= f.submit value %>
  <% if value == '更新' %>
    <%= link_to '取消', new_post_path, data: {remote: true} %>
  <% end %>
<% end %>
